# Live Experiments

Live experiments are out of scope for the next phase. No live result is claimed unless a future explicitly reopened scope produces a saved result with `source_mode=live` and a successful provider call.
