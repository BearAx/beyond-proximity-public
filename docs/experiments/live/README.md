# Live Experiments

Live mode requires `SEMANTICSPLAT_PROVIDER`, `SEMANTICSPLAT_MODEL`, and `SEMANTICSPLAT_API_KEY`. No live result is claimed unless the saved result has `source_mode=live` and the provider call succeeded.
