# E2E product demo video

## What was done

- `scripts/render_e2e_product_demo.py` — 30s MP4: Navigator → Capture → Ask → Query Flow steps → found view with bbox
- `scripts/record_e2e_product_demo.py` — optional Playwright live browser recording
- Output: `docs/benchmark_results/e2e_product_demo.mp4`
- Wired into `run_full_benchmark.py`

## Query used

`Find the sofa` → found **v004** with bbox overlay.

## Regenerate

```bash
PYTHONPATH=. python scripts/render_e2e_product_demo.py --query "Find the sofa"
open docs/benchmark_results/e2e_product_demo.mp4
```
